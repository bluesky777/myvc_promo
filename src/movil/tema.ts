/*
 * LOS COLORES DE `myvc_flutter`, de `lib/constantes.dart`. **No son los de la web**: la app tiene su
 * morado y no el color que elige cada colegio, porque es UNA sola app para los quince.
 */
export const PRIMARIO = '#6A62B7';
export const FONDO = '#E5E5E5';

/*
 * Los dos con los que se señala una falta a la institución. Son de la misma familia que el color de
 * la app **para que se lean como dos grados de lo mismo** y no como dos avisos sin relación: rosa el
 * que llegó tarde, morado el que no vino.
 */
export const TARDANZA = '#FF7FA8';
export const AUSENCIA = '#B47CE0';

export const TINTA = '#141414';
export const TENUE = 'rgba(0,0,0,.54)';
export const APAGADO = 'rgba(0,0,0,.30)';
export const LINEA = 'rgba(0,0,0,.09)';
export const BLANCO = '#ffffff';

export const FUENTE =
	'-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';

/* La pantalla, en puntos. El teléfono se dibuja alrededor. */
export const PANTALLA = { ancho: 420, alto: 912 };
export const MARCO = 13;
export const ESTADO = 46;
export const BARRA = 64;
